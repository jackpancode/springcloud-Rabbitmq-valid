server:
  port: 8762
spring:
  application:
    name: config-server-8762
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
